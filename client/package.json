{
  "name": "mcp-game-client",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^27",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
